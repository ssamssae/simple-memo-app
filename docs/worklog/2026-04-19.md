# 2026-04-19 작업일지

TL;DR: 메모요 Android 선택 박스 2톤 이슈 드림팀 분석·해결 + 상단 버튼 전면 정리(하트/X/알약 제거, fontSize 16 통일) + APK 48.9MB → 16.7MB 최적화.

[메모요] 텍스트 편집 UX 튜닝 — 선택 블록 높이 `BoxHeightStyle.max` → `tight` → `includeLineSpacingMiddle`, 줄 간격 1.35 → 1.5, 선택 핸들 반지름 10 → 7
[메모요] Listener `onPointerUp` 추가로 드래그·탭 종료 좌표 캡처
[메모요] 컨텍스트 메뉴 앵커 `primary + secondary` 탭 좌표로 고정 + "Select All" 버튼 추가 (전체 선택 시 자동 숨김)
[메모요] `main.dart` 글로벌 `textSelectionTheme` 추가 (cursor/selection/handle amber 통일)
[메모요] Android 선택 박스 2톤 이슈 — `BoxHeightStyle.max` 분기 제거, 모든 플랫폼 `includeLineSpacingMiddle` 로 통일 (`memo_edit_screen.dart:442`)
[메모요] 상단 버튼 정리 — 뒤로/취소/저장/편집·취소 토글에서 하트(`Icons.favorite`)·X(`Icons.close`)·pill 테두리 제거
[메모요] 텍스트 버튼 `fontSize` 11 → 16 통일 (편집 모드 "삭제" 버튼과 크기 일치)
[메모요] `_pillButton` 의 `icon` 파라미터 선택적 전환 + 고정 `width: 58` 제거
[메모요] APK 최적화 — 48.9MB → 16.7MB (`--target-platform android-arm64` + `--obfuscate --split-debug-info`)
[메모요] Play App Signing 키 vs 업로드 키 서명 불일치 이슈 파악 — 로컬 APK 설치 시 Play 스토어 버전 선제거 필요
[메모요] iPhone `/irun` 10회 + APK 릴리즈 3회 빌드·텔레그램 전송 (실기기 반복 검증)

[협업] 드림팀 3 에이전트 병렬 호출 — Flutter 렌더링 원인분석 / UX 색상 대안 5종 / 코드 터치포인트 전수조사
[학습] Flutter 선택 박스 드로잉 구조 — `_TextHighlightPainter.paint` 가 줄별 rect 를 개별 `canvas.drawRect` 로 호출 → 1px 겹칠 때 alpha 누적(0.35 → 0.577) = 줄 경계 진한 띠
[학습] `BoxHeightStyle.max` + `height: 1.5` + `leadingDistribution.even` 조합이 줄 경계 overlap 유발, `includeLineSpacingMiddle` 은 사각형 분리로 회피
[학습] `strutStyle.forceStrutHeight` + `TextSelectionToolbarAnchors` primary/secondary anchor 의미 + Cupertino 페이지네이션 동작 정리

[인프라] `cc` 래퍼(`~/bin/cc`) tmux grouped session 구조 확인 — Mac Ghostty ↔ iPhone Termius 터미널 사이즈 독립 유지 + `--channels plugin:telegram@claude-plugins-official` 자동 주입
[인프라] MEMORY 업데이트 — `reference_telegram_restart_command.md` 에 `cc -c` 래퍼 사용법 반영, 인덱스 한 줄 교체
[인프라] `~/todo/todos.md` — 메모요 세부조정 3종(복붙 엔터·메뉴 위치·블럭선택 메뉴) 완료 처리 후 커밋

## 블로커
- 없음

## 내일
- Apple 심사 결과 확인 (재심사 대기중, 2026-04-17 제출)
- Play Closed Testing Day 5(~2026-04-22) 1차 업데이트 aab 업로드 — 오늘 수정분 묶어서
- 메모요 iOS UIPasteControl 이식 (MethodChannel)

## 주요 커밋
- `0c0ad2e` todo: 메모요 세부조정 3종 완료 반영
- `5ba235d` daejong-page: 작업일지 2026-04-19 동기화 (어제 밤 스냅샷)
- (simple_memo_app 커밋은 이번 푸시 후 추가)

https://github.com/ssamssae/daejong-page/blob/main/worklog/2026-04-19.md
